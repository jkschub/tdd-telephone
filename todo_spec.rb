require "rspec"

describe Todo do
	# let(:task) { task = Todo.new() }

	context "when initializing" do
		it "should have a title" do
		end

		it "should have a description" do
		end

		it "should have a status" do
		end

		it "should have a created_at timestamp" do
		end
	end
end